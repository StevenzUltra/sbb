# Task M3-readme (lead): bilingual README with real screenshots

After h2 delivers `docs/reports/m3-h2-screens/` (four states x two themes, 1440x900) and the
20 s demo recording:

- `README.md` (English) and `README.zh.md` (中文), same structure: one-sentence positioning
  ("several AI subscriptions, on separate accounts, working as one team"), a hero image of the
  console, a 20 s GIF (spawn -> team channel ask -> reply with receipt -> drag-to-transfer ->
  moderated approval), how it works (tmux as host, three delivery channels, four-state receipts,
  unique brain ids, policy, quota), install (`npm i -g switch-brain-brain`, command `sbb`; the npm
  name `sbb` is taken), quick start (five commands), FAQ (why not turn a CLI into an API key,
  what happens when a CLI updates, does it need Ghostty), status and roadmap, license.
- Screenshots are the real web console in fixture mode, never the design mockups.
- GitHub repo settings: description in both languages, topics (`ai-agents`, `tmux`, `claude-code`,
  `codex`, `multi-agent`, `orchestration`), social preview image = the light console screenshot.
