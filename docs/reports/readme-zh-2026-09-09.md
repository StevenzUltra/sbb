# README 中文翻译交付报告

- 任务：`5254d773`。
- 执行者：ios#SSL-0047；上级：lead#SSL-0045。
- 源文件：`/Users/steven/developer/sbb/README.md`。
- 交付文件：`/Users/steven/developer/sbb/docs/README.zh.md`。

## 完成内容

已将代码块之外的英文说明、标题、列表和表格译成中文，保留项目名、产品名、技术标识、命令及版本号。两个代码块均逐字保留，包括目录说明中的英文。源文件未修改。

## 验证

执行 `python3` 内联脚本，直接读取源文件与译文并对比，结果如下：

- 源文件 SHA-256 与翻译前记录一致：通过。
- 两个代码块逐字一致：通过。
- 所有行内代码及其顺序一致：通过。
- Markdown 链接目标一致：通过；原文链接数为 0。
- 逐行结构一致：通过；两份文件均为 56 行、5 个标题，空行、列表及续行、表格列数和代码围栏位置保持一致。
- 文件末尾换行：通过。

执行 `git diff --no-index -- README.md docs/README.zh.md` 并人工逐行复核译义；退出码 1 表示两份文件存在预期的翻译差异。未运行与纯文档翻译无关的应用测试。

源文件 SHA-256：`4bdef1057595fecdd9b4f97c3b87892230947269c18950b517cee15101fbb004`。

译文 SHA-256：`c9f712bc0615d768a15ccf91a45ab5934b9fcbe987268a1cc14ad1cf145629fd`。

## 交付边界

按本次指定的绝对路径交付本地文件，未创建提交、推送、开 PR 或合并。仓库原有的未跟踪文件 `plan.json`、`task-translate.txt` 保持不变。没有遗留的翻译问题。

已通过 `sbb reply 5254d773` 回报。回复消息为 `1149af41`；SBB 返回 `queued via=uds`，4 秒内未取得 `peer_message_status`，因此仅确认入队，尚未确认上级读取。
