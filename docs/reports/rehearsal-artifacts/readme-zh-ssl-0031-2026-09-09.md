# README 中文翻译交付报告

- 执行者：ios#SSL-0031；上级：lead#SSL-0029。
- 原任务：c6b9b0b6；采用候选授权：687adcdf。
- 原文：`/Users/steven/developer/sbb/README.md`
- 交付：`/Users/steven/developer/sbb/docs/README.zh.md`
- 结论：指定路径已落地核验通过的完整中文译文；所有代码块原样保留。

## 本轮实际改动

接手时目标已有 ios#SSL-0023 创建、ios#SSL-0027 核验、review#SSL-0028 修订的译文。本轮逐段核对中文正文并保留既有措辞，先准备候选和差异报告，再经 SBB 获得 lead 明确授权：“采用候选，确保代码块原文不被翻译，其他部分保持现有版本”。

仅将目录代码块的九行说明恢复为当前英文 README 原文；测试代码块 `npm test` 本来即正确，保持不变。代码块之外全部内容逐字保留，未把其他脑的既有翻译记作本轮新增。

## 实际验证

临时 Python 直接读取当前原文、候选与目标文件，完成写入前并发变化防护与写后读回；候选核验输出：

```text
PASS: code blocks exactly match source
PASS: heading levels preserved
PASS: inline code tokens preserved
PASS: line and blank line layout preserved
PASS: list and table row counts preserved
PASS: final newline preserved
PASS: target readback matches candidate bytes
PASS: all content outside code blocks unchanged
PASS: source and target unchanged since preparation before writing
PASS: whitespace check, exit 1 with empty stdout and stderr
```

空白检查命令：

```sh
git diff --no-index --check /dev/null /Users/steven/developer/sbb/docs/README.zh.md
```

退出码 1 表示新文件与 `/dev/null` 存在差异，无空白错误输出。两份文件均为 56 行，一个 H1、四个 H2、两个代码块、四条里程碑和四种投递状态。

人工核对了 tmux-only、隔离账户、官方 CLI、不调用模型 API、不接触凭据、M1 至 M4、四种投递状态、仅一次 Enter 重试且不重发消息、Node 22.13 及测试命令。原文没有 Markdown 链接，译文未增删链接。

SHA-256：

```text
README.md: 4bdef1057595fecdd9b4f97c3b87892230947269c18950b517cee15101fbb004
docs/README.zh.md: 8a7ec946d04e427d2dca46d837502198e4fd11469ac47f84aad815fedf1e1e75
```

机器读回记录：`/tmp/ssl-0031-translation/final-verification.json`。本轮前后差异：`/tmp/ssl-0031-translation/change.diff`。

## 交付边界

- 写入前已登记译文、报告与临时候选路径的 SBB claim。
- 仅修改指定译文并新增本报告；原 README、旧报告、plan.json、task-translate.txt 和其他协作者文件均未修改。
- 按 lead 明确指定的绝对路径落地；未指定 worktree/分支，本轮未切换分支、提交、推送、创建 PR 或合并。Git 收尾交 lead 处理；PR URL 无。
- 纯 Markdown 翻译使用人工语义对照、结构检查、空白检查和目标文件读回；未运行运行时测试、未新增形式化测试、未运行 Markdown 渲染器。

## SBB 交接

- 已通过 `sbb reply c6b9b0b6` 发送完成通知，消息 ID `237b5b39`。返回 `queued`，传输 `uds`；4 秒内未收到 peer_message_status，因此不声称 lead 已读取。
- 译文写入已完成，已释放目标文件 claim，供上级审阅或继续调度。
