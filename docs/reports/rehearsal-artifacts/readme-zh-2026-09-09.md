# README 简体中文翻译报告

- 执行者：ios#SSL-0023
- 上级：lead#SSL-0021
- 任务消息：8515e5df
- 重复任务消息：728ed642；已读回源文件与译文，内容与上次交付一致，无需重写。
- 源文件：`/Users/steven/developer/sbb/README.md`
- 交付文件：`/Users/steven/developer/sbb/docs/README.zh.md`

## 完成内容

已翻译 README 全文，包括简介、当前进展、里程碑、投递状态表、目录说明、运行要求和测试入口。
保留 SBB 展开名称、产品名称、CLI 命令、状态值、技术标识符、目录路径、版本与日期。
保留标题层级、列表顺序、表格行数和代码块结构；目录代码块仅翻译说明，测试命令保持原样。
逐段核对了语义，重点确认 queued 不等于 delivered，以及 unverified 仅允许重试一次 Enter、绝不重发消息。

## 验证

读取真实 README 作为原文；未使用 `task-translate.txt` 内状态表存在多余反引号的副本。

执行一次临时 Python 结构核对（`python3 -`，输入为源文件与译文），输出：

```text
PASS: heading levels and order
PASS: inline technical tokens
PASS: code block count
PASS: directory paths and order
PASS: milestone order
PASS: delivery table row count
PASS: test command preserved
PASS: version and date preserved
PASS: final newline
Source: 56 lines; translation: 56 lines
```

执行空白错误检查：

```sh
git -C /Users/steven/developer/sbb diff --no-index --check /dev/null /Users/steven/developer/sbb/docs/README.zh.md
```

无空白错误输出；退出码 1 表示新文件与 `/dev/null` 存在差异。
随后仅将简介中的“基于 tmux”改为“仅通过 tmux 工作”，补足原文 tmux-only 的限定，结构和技术标识符不变。
本任务仅新增 Markdown 文档，没有执行运行时测试，也未添加自证式测试。

## 交付边界

按任务指定的绝对路径落地译文；原 README、`plan.json`、`task-translate.txt` 及其他协作者文件未修改。
已先登记译文和报告的 SBB path claim。
本次 brief 未指定任务分支或 worktree；未切换分支、提交、推送、创建 PR 或合并，交由 lead 审阅并处理 Git 收尾。
未运行 Markdown 渲染器；已通过原文对照和结构核对检查 Markdown。
