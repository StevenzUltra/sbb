# README 中文翻译验收报告

- 执行者：ios#SSL-0027
- 上级：lead#SSL-0025
- 任务消息：0f7100ff
- 原文：`/Users/steven/developer/sbb/README.md`
- 交付：`/Users/steven/developer/sbb/docs/README.zh.md`
- 结论：现有译文准确、完整，已按当前原文重新核验，可交由 lead 审阅。

## 本轮工作

接手时目标路径已有完整中文译文，旧报告记录执行者为 ios#SSL-0023。本轮读取实际 README 并逐段核对，确认无需修改；保留既有译文和旧报告。本轮新增的是此验收报告，不将既有翻译记作本轮新增成果。

全文覆盖简介、当前进展、M1 至 M4、四种投递状态、目录结构、Node 运行要求及测试入口。品牌展开名称 Switch Brain Brain.、产品名、命令、状态值和技术标识符均保留。

人工语义核对重点：

- tmux-only 的限制、通过官方 CLI 工作、不调用模型 API 和不接触凭据均完整表达。
- delivered 的两种判据保持“或”的关系；queued 明确是目标繁忙时进入其自己的队列。
- unverified 保留“仅重试一次 Enter，绝不重发消息”的约束。
- blocked 的未发送状态、原因列表和附带原因均完整表达。
- 两个代码块及围栏格式保留；目录块只翻译说明文字，九个目录路径及顺序不变，`npm test` 原样保留。
- 当前原文没有 Markdown 链接；译文也未引入或删除链接。

## 实际验证

在 `/Users/steven/developer/sbb` 执行一次 `python3 -` 临时结构核对，直接读取原文和译文。结果：

```text
PASS: milestone order (M1-M4)
PASS: inline technical tokens
PASS: code block count and fence format (2 blocks)
PASS: directory paths and order (9 paths)
PASS: test command preserved exactly
PASS: delivery table structure and state order
PASS: link destinations and format (0 links in source)
PASS: version, date and required runtime preserved
PASS: product and transport names preserved
PASS: line and blank-line layout preserved (56 lines)
PASS: UTF-8 and final newline
```

首次检查把标题总数误写为 6，导致该项断言失败；原文和译文实际均为一个 H1、四个 H2。修正临时核对中的预期，仅重跑受影响的标题核对及首次未执行的空白与哈希检查，结果：

```text
PASS: heading levels and order (5 headings: one H1 and four H2)
PASS: whitespace check (exit 1, no output)
```

空白检查实际命令为 `git diff --no-index --check /dev/null /Users/steven/developer/sbb/docs/README.zh.md`，无错误输出；退出码 1 表示新文件与 `/dev/null` 有差异。

当前文件 SHA-256：

```text
README.md: 4bdef1057595fecdd9b4f97c3b87892230947269c18950b517cee15101fbb004
docs/README.zh.md: a818b998e155d5f1923a380d17dccd677ad8b7f4d529064dcdaa8d14ada7d856
```

## 交付边界

已登记此报告的 SBB path claim。目标译文、原 README、旧报告、plan.json 和 task-translate.txt 均未修改。

本轮未改产品代码，未运行运行时测试或添加测试文件，未运行 Markdown 渲染器。验证证据为当前原文人工对照、Markdown 结构及空白核对。

本次 brief 仅指定译文绝对路径，未指定任务分支或 worktree；现有译文已满足任务，因此未新建翻译分支、提交、推送、创建 PR 或合并。Git 收尾由 lead 处理；PR URL 无。
