# README 中文翻译复核与修正报告

- 执行者：review#SSL-0028（子脑，上级 lead#SSL-0025）
- 任务：校对并修正 `docs/README.zh.md` 翻译质量，直接修改该文件。
- 源文件：`/Users/steven/developer/sbb/README.md`
- 交付文件：`/Users/steven/developer/sbb/docs/README.zh.md`

## 本轮改动（3 处措辞/准确性修正）

1. **`独立隔离的账户` → `彼此隔离的账户`**：原译“独立 + 隔离”语义重复；改为“彼此隔离的账户”，更贴合 isolated account（账户之间相互隔离）。

2. **`并用回执回答“对方到底收到没有？”。` → `并把“对方到底收到没有？”变成一张回执。`**：原文为 `turns "did it actually receive that?" into a receipt`，强调把“是否送达”的不确定性转化为回执；原译“用回执回答”偏离了“变成回执”的本义，改为更贴切的“变成一张回执”。

3. **`它通过官方 CLI 工作` → `它驱动官方 CLI`**：原文 `It drives the official CLIs`；`drives` 意为直接驱动/操作官方 CLI（而非透过模型 API），改为“它驱动官方 CLI”更准确。

其余内容逐段核对无其他准确性、语法或格式问题，维持原译。

## 验证

- 结构核对：1 个 H1 + 4 个 H2（标题层级与顺序正确）；代码块围栏 4 个（= 2 个代码块）；目录代码块 9 条路径及顺序不变；`npm test` 原样保留。
- 技术标识符保留：Switch Brain Brain.、`tell / ask / reply / collect / watch`、`codex queue`、`tmux send-keys`、`claude-uds`、`codex-queue`、`Usage Guard`、`node:sqlite`、`util.parseArgs`、Node 22.13 均未改动。
- 行数：56 行，末尾换行（0a）正常。
- 空白检查：`git diff --no-index --check /dev/null docs/README.zh.md` 无错误输出（退出码 1 表示新文件与 /dev/null 有差异，属预期）。
- 原文核验：仅以真实 `README.md` 为对照；未采用含多余反引号副本的 `task-translate.txt`。

当前文件 SHA-256：

```text
README.md: 4bdef1057595fecdd9b4f97c3b87892230947269c18950b517cee15101fbb004
docs/README.zh.md: c760516cb340b3641d64665dde6edc4a779b897599a5616ecb1548d9c06bc7cb
```

## 交付边界

- 仅修改 `docs/README.zh.md`（本轮目标），新增本报告；未动原 README、plan.json、task-translate.txt 和既有报告。
- 已登记译文与本报告的 SBB path claim。
- 未切换分支、未提交、未推送、未建 PR、未合并；Git 收尾交由 lead 处理。
- 本任务只涉及 Markdown 文档，未运行运行时测试，未新增自证式测试，未运行 Markdown 渲染器（以原文对照 + 结构/空白核对为准）。
