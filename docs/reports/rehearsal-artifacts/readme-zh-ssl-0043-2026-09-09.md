# README 中文翻译交付报告

- 日期：2026-09-09
- 执行者：ios#SSL-0043
- 上级：lead#SSL-0041
- 任务消息：`0de3f027`；补充要求：`289f09fe`。
- 来源：`/Users/steven/developer/sbb/README.md`
- 产物：`/Users/steven/developer/sbb/docs/README.zh.md`，55 行。

## 完成内容

已翻译项目介绍、开发状态、M1-M4 里程碑、四种投递状态、Node 版本要求及测试说明。按补充要求原样保留目录结构和测试命令两个英文代码块，并保留产品名称、CLI 命令、状态标识符、目录路径和版本日期。

逐段对照原文核对含义，保留原文的“设计草案 v0.6（2026-09-09）。M1 内核开发中”表述；本任务不判断或更新项目实际开发状态。

## 验证

使用一次性 `python3` 脚本读取两份文档，检查 Markdown 标题数量、代码围栏、内联代码标识符及其出现次数、目录路径顺序、测试命令、版本信息和尾随空格。结果均通过：

- 5 个标题，4 个代码围栏标记（2 个代码块）。
- 9 个目录路径，原文与译文完全一致且顺序相同。
- 两份文档各 20 个内联代码片段，内容及出现次数完全一致。首次检查的输出摘要误写为 18；随后按实际匹配数量读回确认为 20，检查断言本身通过。
- `npm test` 原样保留。
- `v0.6`、`2026-09-09`、`Node 22.13` 和 M1-M4 均保留。
- 原文无 Markdown 链接；译文无待调整的相对链接。
- 无尾随空格，文件以换行结束。

实际数量读回命令：

```bash
python3 - <<'PY'
from pathlib import Path
import re
for p in [Path('README.md'), Path('docs/README.zh.md')]:
    print(str(p) + ': inline_code_spans=' + str(len(re.findall(r'(?<!`)`([^`\n]+)`(?!`)', p.read_text()))))
PY
```

输出：

```text
README.md: inline_code_spans=20
docs/README.zh.md: inline_code_spans=20
```

`git status --short -- README.md docs/README.zh.md docs/reports/readme-zh-ssl-0043-2026-09-09.md` 在报告创建前输出：

```text
?? docs/README.zh.md
```

README 源文件没有改动。本任务仅为文档翻译，未运行 `npm test` 或新增形式化测试。

## 文件指纹

- 来源 SHA-256：`4bdef1057595fecdd9b4f97c3b87892230947269c18950b517cee15101fbb004`
- 译文 SHA-256：`760aecf34ed8c039fd2ed5fb91dd8e4eec944fdf068e94961155678c82cb9cee`

## 补充任务 289f09fe

已读取 `/private/tmp/claude-501/-Users-steven-developer-eagerstudy/3b37dfe7-99df-4ea7-8f05-75d170718bcb/scratchpad/translate-task.txt`，按“保留代码块和技术术语的英文”调整目录结构代码块，恢复英文原文，并为 main brain、sub brain、roster union、copy-mode 保留英文术语。

使用 `python3` 读取源文和译文，以正则表达式提取 fenced code blocks 后逐字比较，并重新比对内联代码和标题数量。实际输出：

```text
PASS: both fenced code blocks match source exactly; 20 inline code spans match; 5 headings; no trailing whitespace.
source_sha256=4bdef1057595fecdd9b4f97c3b87892230947269c18950b517cee15101fbb004
translation_sha256=760aecf34ed8c039fd2ed5fb91dd8e4eec944fdf068e94961155678c82cb9cee
```

源文件指纹与首次交付相同；上方译文指纹已更新为最终版本。

## 交付边界

按任务明确指定的路径完成本地落盘。已事先登记译文及本报告的 path claim；保留仓库中其他人的未跟踪文件。本轮未创建分支、提交或 PR，未合并、未推送 main；待 lead 汇总审阅。无翻译遗留问题。
